import XCTest
@testable import Eelay

final class EelayTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(Eelay().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
